import React, { useState } from 'react';

function otherFunction(): string {
  // Create other functions to do things
  return ""
}

function IndexPopup(): JSX.Element {
  // Create variables and functions so that your code is interactive
     return (
      {/*React components and HTML code*/}
   )
}

export default IndexPopup